// Function to concatenate two arrays
function concatenateArrays(arr1, arr2) {
  return arr1.concat(arr2);
}

// Example arrays
let array1 = [1, 2, 3];
let array2 = [4, 5, 6];

// Concatenate arrays
let result = concatenateArrays(array1, array2);

console.log(result); // Output: [1, 2, 3, 4, 5, 6]
